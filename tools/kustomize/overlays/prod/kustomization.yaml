apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
- ../../base

patchesStrategicMerge:
- patch.yaml

namePrefix: prod-

commonLabels:
  environment: prod

replicas:
- name: hello-app
  count: 3 